import { NgModule } from '@angular/core';

/**
 * CoreModule
 * ----------
 * Holds singleton services, global providers (interceptors/guards),
 * and app-wide configuration. Should be imported once in the root.
 */
@NgModule({
  providers: [],
})
export class CoreModule {}

